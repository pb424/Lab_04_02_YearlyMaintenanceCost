public class Main {
    public static void main(String[] args) {
        double winterCost = 100.5;
        double springCost = 55.2;
        double summerCost = 79.6;
        double fallCost = 67.1;
        double totalCost = winterCost + springCost + summerCost + fallCost;
        System.out.println("The total cost for the year is $" + totalCost + ", with a cost of $" + winterCost + " for Winter, a cost of $" + springCost + " for Spring, a cost of $" + summerCost + " for Summer, and a cost of $" + fallCost + " for Fall." );
    }
}
